import static org.junit.Assert.assertEquals;
import static org.junit.Assert.assertNotNull;

/**
 * Created by burakdede on 9.11.15.
 *
 * Make request to rest client written in python
 * within the folder rest-api
 *
 * 1- source venv/bin/activate
 * 2- python app.py
 * 3- browse localhost:8888/rest
 *
 */
public class HoodieTest {

    /*private static final String URL = "http://localhost:8888";
    private HoodieRest mockRest;

    @Before
    public void setup() {
        mockRest = Hoodie.registerNewTarget(HoodieRest.class, URL);
    }

    @Test
    public void testGet() {
        String response = mockRest.get();
        assertNotNull(response);
    }

    @Test
    public void testPost() {
        String response = mockRest.post();
        assertNotNull(response);
    }

    @Test
    public void testHead() {
        Response response = mockRest.head();
        assertEquals(response.getStatus(), 200);
    }

    @Test
    public void testDelete() {
        String response = mockRest.delete();
        assertNotNull(response);
    }*/
}
